<div id="header">
  <div class="container center">
    <% if @settings.logo_image.file? %>
      
      <a href="/"><%= image_tag @settings.logo_image.url, alt: "logo" %></a>
    
    <% else %>
    
      <h1><a href="/"><%= @settings.project_name %></a></h1>
    
    <% end %>
  </div>
</div>